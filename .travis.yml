# silly-mathgen-tree .travis.yml

language: java

jdk:
    - openjdk11

addons:
    apt:
        update: true

notifications:
    email:
        recipients:
            - asterios.raptis@web.de
        on_failure: always
